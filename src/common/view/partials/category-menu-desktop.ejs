<!-- categories (desktop) — server-side rendered -->
<div class="relative" x-data="{ open: false }" x-on:click.outside="open = false">
  <button
    type="button"
    class="inline-flex items-center gap-1 text-muted transition-colors hover:text-foreground"
    x-bind:class="open ? 'text-foreground' : ''"
    x-on:click="open = !open"
    aria-haspopup="true"
    x-bind:aria-expanded="open"
  >
    <span class="font-semibold text-sm"><%= t('nav_category') %></span>
    <svg
      xmlns="http://www.w3.org/2000/svg"
      fill="none"
      viewBox="0 0 24 24"
      stroke-width="1.5"
      stroke="currentColor"
      class="w-5 h-5 transition-transform"
      x-bind:class="open ? 'rotate-180' : ''"
    >
      <path stroke-linecap="round" stroke-linejoin="round" d="m19.5 8.25-7.5 7.5-7.5-7.5" />
    </svg>
  </button>

  <div
    class="absolute top-full pt-5 z-30 <%= dir === 'ltr' ? 'left-0' : 'right-0' %>"
    x-show="open"
    x-transition
    x-cloak
    style="display: none"
  >
    <ul class="flex flex-col relative w-56 bg-background border border-border shadow-2xl shadow-black/5">
      <% if (typeof menuCategories !== 'undefined') { %>
      <% menuCategories.forEach(function(item) { %>

      <% if (item.children && item.children.length > 0) { %>
      <li class="group relative">
        <a
          href="<%= lp %><%= item.href %>"
          class="flex items-center relative text-foreground transition-colors hover:text-primary p-3"
          x-on:click="open = false"
        >
          <span class="font-semibold text-sm"><%= loc(item.title) %></span>
          <span class="absolute <%= dir === 'ltr' ? 'right-3' : 'left-3' %>">
            <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor" class="w-5 h-5 <%= dir === 'ltr' ? 'rotate-180' : '' %>">
              <path stroke-linecap="round" stroke-linejoin="round" d="M15.75 19.5 8.25 12l7.5-7.5" />
            </svg>
          </span>
        </a>
        <ul class="absolute top-0 <%= dir === 'ltr' ? 'left-full' : 'right-full' %> flex flex-wrap flex-col w-96 bg-background border border-border shadow-2xl shadow-black/5 space-y-3 opacity-0 invisible group-hover:opacity-100 group-hover:visible px-3 pt-8 pb-3 z-20">
          <li class="absolute top-2">
            <span class="font-bold text-sm text-muted cursor-default"><%= t('nav_subcategories') %></span>
          </li>
          <% item.children.forEach(function(child) { %>
          <li class="w-1/2">
            <a
              href="<%= lp %><%= child.href %>"
              class="flex items-center gap-2 text-muted before:content-[''] before:inline-block before:w-1 before:h-1 before:bg-border before:rounded-full transition-colors hover:text-primary hover:before:bg-primary py-1"
              x-on:click="open = false"
            >
              <span class="font-semibold text-sm"><%= loc(child.title) %></span>
            </a>
          </li>
          <% }) %>
        </ul>
      </li>
      <% } else { %>
      <li>
        <a
          href="<%= lp %><%= item.href %>"
          class="flex items-center relative text-foreground transition-colors hover:text-primary p-3"
          x-on:click="open = false"
        >
          <span class="font-semibold text-sm"><%= loc(item.title) %></span>
        </a>
      </li>
      <% } %>

      <% }) %>
      <% } %>
    </ul>
  </div>
</div>
